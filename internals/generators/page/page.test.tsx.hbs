import React from 'react';
import { render } from '@testing-library/react';
{{#if wantHeaders}}
import { HelmetProvider } from 'react-helmet-async';
{{/if}}

import {{ properCase ComponentName }} from 'pages/{{ properCase ComponentName }}Page/{{ properCase ComponentName }}Page';
/* -------------------------------------------------------------------------- */

const renderComponent = () =>
  render(
    {{#if wantHeaders}}
    <HelmetProvider>
    {{/if}}
      <{{ properCase ComponentName }}Page />
    {{#if wantHeaders}}
    </HelmetProvider>
    {{/if}}
);

describe('<{{ properCase ComponentName }}Page />', () => {
  it('should match the snapshot', () => {
    const component = renderComponent();
    expect(component.container.firstChild).toMatchSnapshot();
  });
});
